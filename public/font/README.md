## Logo 字体说明

- `Pacifico-Regular.ttf`：Latin 子集（约 13KB），`style.scss` 中 `@font-face` 实际引用的文件
- `UnidreamLED.ttf`：时钟数字字体（Func 组件使用）

如需完整字符集，可自行下载 Pacifico 全量字体并替换 `Pacifico-Regular.ttf`（体积约 315KB，不建议，子集已覆盖 `li'remio` 等常用 Latin 字符）。
