# Architecture Decision Records

当变更影响架构边界、依赖选型、公共配置格式、部署流程或兼容性时，新增 `NNNN-short-title.md`，使用 `docs/ai/templates/adr.md`。

编号按合并顺序递增。ADR 一经采纳不删除；后续变更用新 ADR 标记旧决策为 `superseded`。纯 bug 修复、文案和局部样式通常不需要 ADR。
